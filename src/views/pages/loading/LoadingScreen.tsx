import { LoadingWrapper, Spinner } from "./loadingPage.wrappers";


const LoadingScreen = () => {
    return (
        <LoadingWrapper>
            <Spinner />
        </LoadingWrapper>
    );
};

export default LoadingScreen;
