import { LoadingWrapper, Spinner } from "./loadingPageWrappers";


const LoadingScreen = () => {
    return (
        <LoadingWrapper>
            <Spinner />
        </LoadingWrapper>
    );
};

export default LoadingScreen;
