import { LoadingWrapper, Spinner } from "./loadingPageWrappers";


const LoadingPage = () => {
    return (
        <LoadingWrapper>
            <Spinner />
        </LoadingWrapper>
    );
};

export default LoadingPage;
